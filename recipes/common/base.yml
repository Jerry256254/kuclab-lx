---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: kargs
    kargs:
      - rw
      - quiet
      - splash
      - psi=1

  - type: systemd
    system:
      enabled:
        - fstrim.timer
